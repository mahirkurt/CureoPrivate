// Code Connect stub template — React (.figma.tsx)
// This file is generated by figma-forge in CODE_CONNECT mode.
// Edit the example() function body if the runtime usage differs from the design intent.
//
// Placeholders replaced at generation time:
//   {{COMPONENT_NAME}}    — code component class/function name (e.g., Button)
//   {{COMPONENT_IMPORT}}  — import path relative to the .figma.tsx file
//   {{FIGMA_URL}}         — full Figma node URL
//   {{VARIANT_PROPS}}     — variant property mappings
//   {{EXAMPLE_BODY}}      — JSX example body

import { figma } from "@figma/code-connect";
import { {{COMPONENT_NAME}} } from "{{COMPONENT_IMPORT}}";

figma.connect(
  {{COMPONENT_NAME}},
  "{{FIGMA_URL}}",
  {
    props: {
      {{VARIANT_PROPS}}
    },
    example: (props) => (
      {{EXAMPLE_BODY}}
    )
  }
);
