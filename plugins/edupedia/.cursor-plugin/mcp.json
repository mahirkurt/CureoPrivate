{
  "_comment": "ÜRETİLMİŞ DOSYA — elle düzenlemeyin. Kaynak: fleet.yaml → python3 tools/fleetkit/gen_fleet.py — edupedia MCP wiring. 2 server (2 Bearer-gated, 0 public). Auth'lu server'lar Doppler-injected Bearer bekler; anahtar yoksa o katman graceful degrade eder (asla uydurma). Cursor `${env:VAR}` process-env interpolasyonu (plugin-variable paste formu değil).",
  "mcpServers": {
    "maarif-mufredat": {
      "type": "http",
      "url": "https://mufredat.cureonics.com/mcp",
      "headers": {
        "Authorization": "Bearer ${env:MUFREDAT_MCP_API_KEY}"
      },
      "_fix": "2026-08-06 denetimi: anahtarsız 401, MUFREDAT_MCP_API_KEY ile 200. Bu plugin'in OTORİTE kaynağıdır (105 MEB ders kitabı tam metni) ve 'authless' sanıldığı için arıza preflight'ta görünmüyordu — titck sınıfı sessiz yapılandırma hatası."
    },
    "egitim-kaynak": {
      "type": "http",
      "url": "https://egitim-kaynak.cureonics.com/mcp",
      "headers": {
        "Authorization": "Bearer ${env:EGITIM_KAYNAK_MCP_API_KEY}"
      }
    }
  }
}
