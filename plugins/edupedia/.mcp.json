{
  "_comment": "ÜRETİLMİŞ DOSYA — elle düzenlemeyin. Kaynak: fleet.yaml → python3 tools/fleetkit/gen_fleet.py — edupedia MCP wiring. 3 server (3 Bearer-gated, 0 public). Auth'lu server'lar Doppler-injected Bearer bekler; anahtar yoksa o katman graceful degrade eder (asla uydurma).",
  "mcpServers": {
    "maarif-mufredat": {
      "type": "http",
      "url": "https://mufredat.cureonics.com/mcp",
      "headers": {
        "Authorization": "Bearer ${MUFREDAT_MCP_API_KEY}"
      },
      "_fix": "2026-08-06 denetimi: anahtarsız 401, MUFREDAT_MCP_API_KEY ile 200. Bu plugin'in OTORİTE kaynağıdır (105 MEB ders kitabı tam metni) ve 'authless' sanıldığı için arıza preflight'ta görünmüyordu — titck sınıfı sessiz yapılandırma hatası."
    },
    "egitim-kaynak": {
      "type": "http",
      "url": "https://egitim-kaynak.cureonics.com/mcp",
      "headers": {
        "Authorization": "Bearer ${EGITIM_KAYNAK_MCP_API_KEY}"
      }
    },
    "modul-yayin": {
      "type": "http",
      "url": "https://edupedia.cureonics.com/mcp",
      "headers": {
        "Authorization": "Bearer ${EDUPEDIA_PUBLISH_TOKEN}"
      }
    }
  }
}
