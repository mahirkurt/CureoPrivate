{
  "name": "anamnesis-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Hardened-OAuth Cloudflare Worker MCP — semantic-chunking + vector RAG (Vectorize/bge-m3) + entity-relation graph (D1) for the evidentia plugin. Keeps large full-text corpora out of the model context window; serves bounded, provenance-stamped, graph-grounded evidence bundles.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.80",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.21",
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.6.0",
    "vitest": "~3.2.0",
    "wrangler": "^4.20.0"
  }
}
