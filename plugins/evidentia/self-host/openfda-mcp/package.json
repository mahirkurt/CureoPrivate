{
  "name": "openfda-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Hardened-OAuth Cloudflare Worker MCP — typed openFDA (api.fda.gov) access for the evidentia plugin: FAERS adverse events, SPL drug labeling, drugsfda approvals, enforcement/recalls, device/*. Single tool openfda_search; ICD-11 served separately by med-terminologies. Keyless public FDA REST upstream.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.80",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.3",
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
