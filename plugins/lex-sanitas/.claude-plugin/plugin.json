{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "lex-sanitas",
  "displayName": "Lex Sanitas",
  "version": "3.8.3",
  "description": "Türkiye sağlık mevzuatının her düzlemde (kanun · CBK · Cumhurbaşkanı kararı · yönetmelik · tebliğ · genelge) reform, değişiklik ve yeniden yazımı için en ileri düzey norm-üretim protokolü. Çekirdek amaç: yeni mevzuat üretmek (DRAFT) + mevcut mevzuatı değiştirmek (AMEND) + TBMM kanun teklifi hazırlamak (TBMM_KANUN_TEKLIFI). Destekleyen modlar: analiz · 5210 uyum denetimi · paydaş/bilirkişi görüşü · RIA (DEA/BEF) · karşılaştırmalı hukuk · ex post değerlendirme (9 mod). 5210 sayılı Yönetmelik + AYM belirlilik içtihadı + OECD Better Regulation + Anayasa Md.17/56/90/5 + ICESCR Md.12 çerçevesi; G0-G9 kalite kapıları + R6b 21-nokta yürütülebilir uyum rubriği + evidence_ledger no-fabrication disiplini. TÜRETİLMİŞ FİLO (v3.5.0): 23 hukuk/regülasyon MCP tek bir fleet.yaml kaynağından wire edilir (.mcp.json · lock · hook anahtar haritası · komut tablosu · ajan araç kısıtı ondan üretilir; check_drift.py sürüklenmeyi CI'da yakalar). PRİMER mevzuat + mevzuat-bilgisi + resmi-gazete + titck + tbmm + saglikbakanligi + detsis · KARŞILAŞTIRMALI health-policy [yabancı ülke] + german-law + eurlex [G6 CELEX] + fedlex [CH] + uk-legal + ich + intl-treaty + eudamed + oecd + turk-patent · DOKTRİN yok-akademik + yoktez [tez tam-metin, G7 atıf doğrulaması] + literatur [DergiPark tam-metin] · TAM-METİN ŞELALESİ openathens [Tier 3 lisanslı] → annas-reader [Tier 4 son çare, yalnız analiz] · SUBSTRAT anamnesis. Canlı MCP prob'lu preflight 'anahtar yok' (meşru degrade) ile 'sunucu 401 verdi' (yapılandırma arızası) hâllerini ayırır. Bağlam-tetiklemeli ZORUNLU delegasyon (kuruluysa atlanamaz; yoksa graceful degrade): klinik kanıt → evidentia plugin; AI güvenilirlik + atıf-adli + Türkçe hukuk dili → sci-audit plugin. 3 companion connector (Yargı↔G5 içtihat, Open Law↔UK birincil metin, Ansvar↔Mod7 58-yargı) tam-filonun zorunlu üyeleri — manifesto satırları her çıktıda zorunlu. Kapsam: reform/norm-üretim; bireysel dava (SGK red, AYM başvuru), malpraktis, promosyon denetimi KAPSAM DIŞI (Scope Guard).",
  "author": {
    "name": "Cureonics",
    "url": "https://cureonics.com"
  },
  "license": "Internal skill asset, not for external redistribution",
  "homepage": "https://cureonics.com",
  "keywords": [
    "turkish-health-law",
    "regulatory-drafting",
    "mevzuat-reformu",
    "5210-yonetmelik",
    "kanun-teklifi",
    "yonetmelik-taslagi",
    "teblig-taslagi",
    "regulatory-impact-assessment",
    "comparative-regulation",
    "titck",
    "sgk",
    "aym-belirlilik",
    "oecd-better-regulation",
    "ex-post-evaluation",
    "no-fabrication",
    "evidence-ledger"
  ],
  "repository": "https://github.com/mahirkurt/CureoPrivate",
  "skills": "./skills",
  "commands": "./commands",
  "agents": "./agents",
  "hooks": "./hooks/hooks.json",
  "mcpServers": "./.mcp.json",
  "userConfig": {
    "mevzuat_api_key": {
      "type": "string",
      "title": "mevzuat API key",
      "description": "NOT: .mcp.json header'ı ${MEVZUAT_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${MEVZUAT_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "resmi_gazete_api_key": {
      "type": "string",
      "title": "resmi-gazete API key",
      "description": "NOT: .mcp.json header'ı ${RESMI_GAZETE_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${RESMI_GAZETE_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "titck_api_key": {
      "type": "string",
      "title": "titck API key",
      "description": "NOT: .mcp.json header'ı ${TITCK_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${TITCK_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "tbmm_api_key": {
      "type": "string",
      "title": "tbmm API key",
      "description": "NOT: .mcp.json header'ı ${TBMM_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${TBMM_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "saglikbakanligi_api_key": {
      "type": "string",
      "title": "saglikbakanligi API key",
      "description": "NOT: .mcp.json header'ı ${SAGLIK_BAKANLIGI_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${SAGLIK_BAKANLIGI_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "detsis_api_key": {
      "type": "string",
      "title": "detsis API key",
      "description": "NOT: .mcp.json header'ı ${DETSIS_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${DETSIS_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "health_policy_api_key": {
      "type": "string",
      "title": "health-policy API key",
      "description": "NOT: .mcp.json header'ı ${HEALTH_POLICY_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${HEALTH_POLICY_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "german_law_api_key": {
      "type": "string",
      "title": "german-law API key",
      "description": "NOT: .mcp.json header'ı ${GERMAN_LAW_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${GERMAN_LAW_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "ich_guidelines_api_key": {
      "type": "string",
      "title": "ich-guidelines API key",
      "description": "NOT: .mcp.json header'ı ${ICH_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${ICH_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "intl_treaty_api_key": {
      "type": "string",
      "title": "intl-treaty API key",
      "description": "NOT: .mcp.json header'ı ${INTL_TREATY_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${INTL_TREATY_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "eudamed_api_key": {
      "type": "string",
      "title": "eudamed API key",
      "description": "NOT: .mcp.json header'ı ${EUDAMED_MCP_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${EUDAMED_MCP_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "oecd_api_key": {
      "type": "string",
      "title": "oecd API key",
      "description": "NOT: .mcp.json header'ı ${OECD_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${OECD_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "yok_akademik_api_key": {
      "type": "string",
      "title": "yok-akademik API key",
      "description": "NOT: .mcp.json header'ı ${YOK_AKADEMIK_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${YOK_AKADEMIK_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "openathens_api_key": {
      "type": "string",
      "title": "openathens API key",
      "description": "NOT: .mcp.json header'ı ${OPENATHENS_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${OPENATHENS_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "annas_reader_api_key": {
      "type": "string",
      "title": "annas-reader API key",
      "description": "NOT: .mcp.json header'ı ${ANNAS_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${ANNAS_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "eurlex_api_key": {
      "type": "string",
      "title": "eurlex API key",
      "description": "NOT: .mcp.json header'ı ${EURLEX_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${EURLEX_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "fedlex_api_key": {
      "type": "string",
      "title": "fedlex API key",
      "description": "NOT: .mcp.json header'ı ${FEDLEX_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${FEDLEX_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "uk_legal_api_key": {
      "type": "string",
      "title": "uk-legal API key",
      "description": "NOT: .mcp.json header'ı ${UK_LEGAL_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${UK_LEGAL_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "anamnesis_api_key": {
      "type": "string",
      "title": "anamnesis API key",
      "description": "NOT: .mcp.json header'ı ${ANAMNESIS_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${ANAMNESIS_MCP_API_KEY} olarak da dışa aktarmanız gerekir (ör. `doppler run -p cureohub -c dev_personal -- claude`); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    }
  }
}
