{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "vekayinuvis",
  "displayName": "Vekayinüvis — Osmanlı/Türk Tarih Araştırma Protokolü",
  "version": "3.4.2",
  "description": "Birincil-kaynak-öncelikli Osmanlı/Türk tarih araştırma orkestratörü. TAM-FİLO (17 server): Ottoman Archives + resmî Devlet Arşivleri kataloğu (BOA/BCA/Diplomatik/Askeri — fon/kutu/gömlek, artık 27 araç: arama 5'lisi + süpürme/store 3'lüsü + belge 4'lüsü + eSatış sepeti 4'lüsü + satın-alınmış/yerel-arşiv 7'lisi + async OCR 2'lisi + durum 2'lisi) + YÖK Tez çekirdek katmanını DergiPark tam-metin (Literatür MCP), YÖK Akademik, akademik triangülasyon (Paper Search, Consensus, Scholar Gateway, Exa, Tavily), kitap/makale tam-metin şelalesi (OpenAthens lisanslı Millet Kütüphanesi + Anna's Reader son-çare) ve yasama/mevzuat katmanı (Resmî Gazete erken-Cumhuriyet arşivi, mevzuat.gov.tr mülga/gerekçe, TBMM yasama soyağacı, DETSİS kurumsal prosopografi) ile birleştirir; anamnesis RAG/GraphRAG substratı + arsiv-tarama-distilleri alt-ajanı ile bağlam ekonomisi (detaylar atlanmadan pencere taşmadan), G0 kapsam manifestosu ile tam-filo garantisi. v3.0: devlet-arsivleri 10→22 araca genişledi — eSatış sepet→noVNC satın-alma (ödeme DAİMA insan, asla otonom) → satın-alınmış belgenin yerel BOA-kodlu arşivinden 300 DPI arşiv-öncelikli okuma → OCR/HTR (`engine=auto|both|transkribus|escriptorium|transleyt|tesseract`, Osmanlı varsayılanı `transleyt` — ölçülen en iyi okuyucu; en doğru okuma Transleyt+görü uzlaştırması) → async OCR job kuyruğu zinciri; üç yeni akış-skill'i (`satinalma`, `arsiv-oku`, `toplu-okuma`); filo 13→17 server'a genişledi (yasama/mevzuat katmanı + DETSİS); komutlar önek-siz skill mimarisine göçtü (`/vekayinuvis:<ad>` — eski `/vekayinuvis:vekayinuvis-<ad>` biçimi BREAKING kaldırıldı). IJMES/TDV İA çeviriyazı ve fon/kutu/gömlek atıf disipliniyle 9 çalışma modu sunar.",
  "author": {
    "name": "Cureonics",
    "url": "https://cureonics.com"
  },
  "homepage": "https://github.com/mahirkurt/CureoPrivate/tree/main/plugins/vekayinuvis",
  "repository": "https://github.com/mahirkurt/CureoPrivate",
  "license": "UNLICENSED",
  "keywords": [
    "ottoman-history",
    "turkish-history",
    "osmanli-arsiv",
    "boa",
    "vakfiye",
    "muhimme",
    "tahrir",
    "seriye-sicili",
    "salname",
    "htr",
    "escriptorium",
    "ebced",
    "hicri-rumi-miladi",
    "prosopografi",
    "kanun-gerekcesi",
    "academic-research",
    "yoktez",
    "ijmes-transliteration",
    "devlet-arsivleri",
    "bca-cumhuriyet-arsivi",
    "fon-kutu-gomlek",
    "dergipark",
    "yok-akademik",
    "anamnesis-rag",
    "openathens",
    "annas-reader",
    "kitap-makale-tam-metin",
    "millet-kutuphanesi",
    "tam-filo",
    "baglamlarda-context-economy",
    "esatis-sepet",
    "novnc-satinalma",
    "transleyt-ocr",
    "iki-okuyucu-uzlastirma",
    "async-ocr",
    "resmigazete",
    "mulga-mevzuat"
  ],
  "userConfig": {
    "smithery_api_key": {
      "type": "string",
      "title": "Smithery API key",
      "description": "Paper Search (Smithery host) için API anahtarı — girilmezse paper-search graceful degrade eder (diğer akademik katman devrede).",
      "sensitive": true,
      "required": false
    },
    "smithery_profile": {
      "type": "string",
      "title": "Smithery profile",
      "description": "Smithery profil tanımlayıcısı (opsiyonel).",
      "required": false
    },
    "devarsiv_api_key": {
      "type": "string",
      "title": "Devlet Arşivleri API key",
      "description": "devlet-arsivleri MCP anahtarı — NOT: .mcp.json header'ı ${DEVARSIV_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş, bkz. code.claude.com/docs/en/plugins-reference). Buraya değer girerseniz AYNI ZAMANDA onu ${DEVARSIV_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir (ör. Doppler/secrets.env); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "ottoman_api_key": {
      "type": "string",
      "title": "Ottoman Archives API key",
      "description": "ottoman-archives MCP anahtarı — NOT: .mcp.json header'ı ${OTTOMAN_ARCHIVES_MCP_API_KEY} ortam değişkenini okur (userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${OTTOMAN_ARCHIVES_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir; yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "yok_akademik_api_key": {
      "type": "string",
      "title": "YÖK Akademik API key",
      "description": "yok-akademik MCP anahtarı — NOT: .mcp.json header'ı ${YOK_AKADEMIK_MCP_API_KEY} ortam değişkenini okur (userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${YOK_AKADEMIK_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir; yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "openathens_api_key": {
      "type": "string",
      "title": "OpenAthens API key",
      "description": "openathens MCP anahtarı — NOT: .mcp.json header'ı ${OPENATHENS_MCP_API_KEY} ortam değişkenini okur (userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${OPENATHENS_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir; yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "annas_api_key": {
      "type": "string",
      "title": "Anna's Reader API key",
      "description": "annas-reader MCP anahtarı — NOT: .mcp.json header'ı ${ANNAS_MCP_API_KEY} ortam değişkenini okur (userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${ANNAS_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir; yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "anamnesis_api_key": {
      "type": "string",
      "title": "Anamnesis API key",
      "description": "anamnesis MCP anahtarı — NOT: .mcp.json header'ı ${ANAMNESIS_MCP_API_KEY} ortam değişkenini okur (userConfig→env fallback sözdizimi belgelenmemiş). Buraya değer girerseniz AYNI ZAMANDA onu ${ANAMNESIS_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir; yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "resmigazete_api_key": {
      "type": "string",
      "title": "Resmî Gazete API key",
      "description": "resmigazete MCP anahtarı — NOT: .mcp.json header'ı ${RESMI_GAZETE_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş, bkz. code.claude.com/docs/en/plugins-reference). Buraya değer girerseniz AYNI ZAMANDA onu ${RESMI_GAZETE_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir (ör. Doppler/secrets.env); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "mevzuat_api_key": {
      "type": "string",
      "title": "Mevzuat API key",
      "description": "mevzuat MCP anahtarı — NOT: .mcp.json header'ı ${MEVZUAT_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş, bkz. code.claude.com/docs/en/plugins-reference). Buraya değer girerseniz AYNI ZAMANDA onu ${MEVZUAT_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir (ör. Doppler/secrets.env); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "tbmm_api_key": {
      "type": "string",
      "title": "TBMM API key",
      "description": "tbmm MCP anahtarı — NOT: .mcp.json header'ı ${TBMM_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş, bkz. code.claude.com/docs/en/plugins-reference). Buraya değer girerseniz AYNI ZAMANDA onu ${TBMM_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir (ör. Doppler/secrets.env); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    },
    "detsis_api_key": {
      "type": "string",
      "title": "DETSİS API key",
      "description": "detsis MCP anahtarı — NOT: .mcp.json header'ı ${DETSIS_MCP_API_KEY} ortam değişkenini okur (host'ta userConfig→env fallback sözdizimi belgelenmemiş, bkz. code.claude.com/docs/en/plugins-reference). Buraya değer girerseniz AYNI ZAMANDA onu ${DETSIS_MCP_API_KEY} ortam değişkeni olarak da dışa aktarmanız gerekir (ör. Doppler/secrets.env); yalnız bu alanı doldurmak header'ı beslemez.",
      "sensitive": true,
      "required": false
    }
  },
  "mcpServers": "./.mcp.json",
  "defaultEnabled": false
}
